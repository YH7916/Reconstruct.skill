# GitHub Launch Kit

Use this file when publishing the repo, writing the About box, or generating social preview images.

## Suggested Repo Name

`reconstruct-skill-suite`

## Suggested Tagline

Route-first AI skills for refactoring risky legacy code with git-safe waves.

## Suggested About Text

AI skills for refactoring legacy code without blowing up tightly coupled systems. Plan the route, get approval, execute in verified waves, and checkpoint every step with git.

## Suggested Topics

- `ai`
- `agent-skills`
- `codex`
- `legacy-code`
- `refactoring`
- `technical-debt`
- `developer-tools`
- `automation`
- `git`
- `software-engineering`

## Suggested Social Post

I built an AI skill suite for the kind of refactor everyone dreads:

- dense dependency graphs
- hidden side effects
- shared mutable state
- legacy modules that break when you touch them

Instead of "rewrite everything", it does:

1. map the blast radius
2. lock behavior
3. show the route
4. wait for approval
5. execute in git-checkpointed waves

Repo: <your-url>

## Suggested Pinned Demo Order

1. Open the scary target file tree
2. Run `$gsd-legacy-refactor ...`
3. Show `.planning/refactors/<slug>/04-WAVE-PLAN.md`
4. Show route approval
5. Show the first checkpoint commit
6. End on `07-EXECUTION-LOG.md`

## Image Prompt: GitHub Social Preview

Use this prompt in your image tool:

```text
Create a clean, high-contrast GitHub social preview image for an open-source developer tool called "Reconstruct Skill Suite".

Visual concept:
- a dangerous legacy dependency graph on the left, dense and tangled
- a clear staged refactor route on the right, broken into safe waves
- subtle git checkpoint markers between waves
- terminal / engineering aesthetic, but polished and modern
- dark slate, electric blue, muted amber accents
- minimal text, only:
  Reconstruct Skill Suite
  Route-first legacy refactoring

Style:
- premium software tooling banner
- crisp typography
- schematic lines and node diagrams
- not cartoonish
- not cluttered
- looks credible to senior engineers on GitHub
- 1280x640 aspect ratio
```

## Image Prompt: README Hero Screenshot Mockup

```text
Generate a polished product mockup for a developer tool that plans safe legacy-code refactors.

Composition:
- left panel: terminal showing command `$gsd-legacy-refactor src/auth --goal "split auth orchestration from token storage"`
- center panel: risk classification with "Risk: high" and "Mode: untangle-first"
- right panel: a markdown wave plan titled `04-WAVE-PLAN.md`
- small git commit badge near the bottom saying "wave 1 checkpoint"

Visual style:
- modern engineering tool UI
- dark background with blue-gray panels
- white text with restrained cyan highlights
- realistic terminal typography
- product screenshot aesthetic, not illustration-heavy
- suitable for a README hero image
```

## Image Prompt: GIF / Short Demo Storyboard

```text
Create a 4-scene storyboard for a developer tool demo about safe AI-assisted legacy refactoring.

Scene 1:
- messy legacy module tree labeled auth, token store, session service, middleware
- caption: "legacy code with dangerous coupling"

Scene 2:
- terminal command running `$gsd-legacy-refactor src/auth`
- caption: "map blast radius before editing"

Scene 3:
- markdown plan showing `untangle-first`, wave 1 boundary, verification gate
- caption: "approve the route"

Scene 4:
- git checkpoint commit after wave 1
- caption: "execute in verified waves"

Style:
- premium devtools motion design
- cinematic but practical
- suitable as a reference for animating a short product GIF
```

## Launch Checklist

- Add repo description and topics
- Upload social preview image
- Pin one demo screenshot or GIF near the top of README
- Publish at least one real before/after demo artifact
- Add a short release note for v0.1.0
